require "test_helper"

class GetweatherReportTest < ActiveSupport::TestCase
  setup do
    # clear rails cache so we can test caching before/after
  end

  test "#process provides the weather report for a given zip, along with cached at time for display purposes" do
    assert false
  end

  test "self.process creates a new instance and calls process on it" do
    assert false
  end
end
